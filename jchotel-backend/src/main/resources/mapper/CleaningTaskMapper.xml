<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jchotel.mapper.CleaningTaskMapper">

    <resultMap id="CleaningTaskMap" type="com.jchotel.entity.CleaningTask">
        <id property="id" column="id"/>
        <result property="roomId" column="room_id"/>
        <result property="roomNo" column="room_no"/>
        <result property="orderId" column="order_id"/>
        <result property="assigneeId" column="assignee_id"/>
        <result property="assigneeName" column="assignee_name"/>
        <result property="status" column="status"/>
        <result property="priority" column="priority"/>
        <result property="remark" column="remark"/>
        <result property="finishTime" column="finish_time"/>
        <result property="inspectTime" column="inspect_time"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="findList" resultMap="CleaningTaskMap">
        SELECT ct.*, r.room_no, u.real_name as assignee_name
        FROM t_cleaning_task ct
        LEFT JOIN t_room r ON ct.room_id = r.id
        LEFT JOIN sys_user u ON ct.assignee_id = u.id
        <where>
            <if test="status != null and status != ''">
                AND ct.status = #{status}
            </if>
            <if test="roomNo != null and roomNo != ''">
                AND ct.room_no = #{roomNo}
            </if>
            <if test="assigneeId != null">
                AND ct.assignee_id = #{assigneeId}
            </if>
            <if test="priority != null and priority != ''">
                AND ct.priority = #{priority}
            </if>
            <if test="keyword != null and keyword != ''">
                AND (ct.room_no LIKE CONCAT('%', #{keyword}, '%') OR ct.remark LIKE CONCAT('%', #{keyword}, '%') OR u.real_name LIKE CONCAT('%', #{keyword}, '%'))
            </if>
        </where>
        ORDER BY ct.create_time DESC
        LIMIT #{offset}, #{size}
    </select>

    <select id="count" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM t_cleaning_task ct
        LEFT JOIN t_room r ON ct.room_id = r.id
        LEFT JOIN sys_user u ON ct.assignee_id = u.id
        <where>
            <if test="status != null and status != ''">
                AND ct.status = #{status}
            </if>
            <if test="roomNo != null and roomNo != ''">
                AND ct.room_no = #{roomNo}
            </if>
            <if test="assigneeId != null">
                AND ct.assignee_id = #{assigneeId}
            </if>
            <if test="priority != null and priority != ''">
                AND ct.priority = #{priority}
            </if>
            <if test="keyword != null and keyword != ''">
                AND (ct.room_no LIKE CONCAT('%', #{keyword}, '%') OR ct.remark LIKE CONCAT('%', #{keyword}, '%') OR u.real_name LIKE CONCAT('%', #{keyword}, '%'))
            </if>
        </where>
    </select>

    <update id="update">
        UPDATE t_cleaning_task
        <set>
            <if test="roomId != null">room_id = #{roomId},</if>
            <if test="roomNo != null">room_no = #{roomNo},</if>
            <if test="orderId != null">order_id = #{orderId},</if>
            <if test="assigneeId != null">assignee_id = #{assigneeId},</if>
            <if test="assigneeName != null">assignee_name = #{assigneeName},</if>
            <if test="status != null">status = #{status},</if>
            <if test="priority != null">priority = #{priority},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="finishTime != null">finish_time = #{finishTime},</if>
            <if test="inspectTime != null">inspect_time = #{inspectTime}</if>
        </set>
        WHERE id = #{id}
    </update>

    <select id="findAllByStatus" resultMap="CleaningTaskMap">
        SELECT ct.*, r.room_no, u.real_name as assignee_name
        FROM t_cleaning_task ct
        LEFT JOIN t_room r ON ct.room_id = r.id
        LEFT JOIN sys_user u ON ct.assignee_id = u.id
        WHERE ct.status = #{status}
        ORDER BY ct.create_time ASC
    </select>

</mapper>
